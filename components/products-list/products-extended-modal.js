import { useState } from 'react';

import Modal from '../modal';

const ProductExtendedModal = ({ ...props }) => {
  return <Modal {...props}>product</Modal>;
};

export default ProductExtendedModal;
