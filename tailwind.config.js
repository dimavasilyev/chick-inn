const { colors } = require('tailwindcss/defaultTheme');

module.exports = {
  theme: {
    container: {
      center: true,
    },
    extend: {
      colors: {
        yellow: '#ECBE02',
        red: '#D31116',
      },
    },
  },
};
